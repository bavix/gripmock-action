name: build app

on:
  pull_request:
    branches: [ master ]

jobs:
  ui:
    permissions:
      contents: write
    uses: bavix/.github/.github/workflows/node-compile-assets.yml@0.5.0
    secrets: inherit
    with:
      nodejs: 24.x
